# Throughline frontend

React + Vite + Tailwind (shadcn/ui) UI for the Throughline character-asset
pipeline. Talks to the FastAPI backend under `/api` (proxied to :8000 in dev).

```
npm install
npm run dev   # http://localhost:5174
```
