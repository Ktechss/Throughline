# Agent notes

This is the Throughline frontend (React + Vite + Tailwind, shadcn/ui components
in `src/components/ui`). Data comes from the FastAPI backend via `src/api/throughline.js`.
No auth, no external SDK — single-user local tool.
